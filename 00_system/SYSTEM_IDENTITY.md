# System Identity

```text
Input  = one or more Track DB documents
Function = Candidate Method Field + Selector + Validators
Output = Hash Data lineage + promoted Hash DB document
```

LRSDoNet_B는 Track DB 원천을 개작하지 않는다.

# System Area Verification

이번 단계에서는 다음 구현조건만 확인한다.

- 모든 필수 경로가 실제 존재하는가
- YAML 문서가 구문상 유효한가
- Function ID가 Registry와 Method에서 일치하는가
- Pipeline의 Function ID가 Registry에 존재하는가
- Schema Index가 실제 Schema 파일을 참조하는가
- Guard ID가 중복되지 않는가
- 빈 핵심파일이 없는가

설계의 타당성을 다시 검토하지 않는다.

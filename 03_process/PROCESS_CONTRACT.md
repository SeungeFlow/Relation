# Process Contract

```text
Data_n
→ Method Lineup_n
→ Partial Results_n
→ Relation Validation_n
→ Result_n
→ Freeze/Rebind
→ Data_n+1
```

각 단계에서 새 `<hash.code.file.name>` 객체를 만들 수 있다. 이전 객체를 덮어쓰지 않는다.
